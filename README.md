# Dragon Stüdyo

Minimal Next.js (App Router) starter for [dragon-studyo.vercel.app](https://dragon-studyo.vercel.app).

```bash
npm install
npm run dev
```

Production build: `npm run build` then `npm start`.
